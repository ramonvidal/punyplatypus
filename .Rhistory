org.Mm.eg.db
library("org.Mm.eg.db")
org.Mm.eg.db
as.list(org.Mm.eg.db)
res <- biomaRt::getBM(attributes = c("ensembl_gene_id", "mgi_symbol","chromosome_name",'strand','transcript_start','transcript_end'), mart = mouse)
mouse = biomaRt::useMart("ensembl", dataset = "mmusculus_gene_ensembl")
res <- biomaRt::getBM(attributes = c("ensembl_gene_id", "mgi_symbol","chromosome_name",'strand','transcript_start','transcript_end'), mart = mouse)
res
n_genes=10
sample(res$mgi_symbol, n_genes)
as.data.frame(sample(res$mgi_symbol, n_genes))
colnames(res)<-"gene"
res
res<-as.data.frame(sample(res$mgi_symbol, n_genes))
res
as.data.frame(sample(res$mgi_symbol, n_genes))
res <- biomaRt::getBM(attributes = c("ensembl_gene_id", "mgi_symbol","chromosome_name",'strand','transcript_start','transcript_end'), mart = mouse)
res<-as.data.frame(sample(res$mgi_symbol, n_genes))
colnames(res)<-"gene"
res
library(Seurat)
library(dplyr)
library(useful)
library(cowplot)
library(gplots)
library(RColorBrewer)
load(file = "~/bpmc_GroupB.RData")
load(file = "~/PBMCs/bpmc_GroupB.RData")
pbmc2<-pbmc
cells<-c(names(pbmc@ident[pbmc@ident=="Fibroblasts1"]), names(pbmc@ident[pbmc@ident=="Fibroblasts2"]))
heart_fib<-SubsetData(object = pbmc2, cells.use = cells, subset.raw = TRUE)
TSNEPlot(object = heart_fib, group.by = "orig.ident", do.return = TRUE, pt.size = 1)
DEGenes<-read.csv("~/Dropbox/Manuscripts/Old/Heart_Aging/Rebutal_NatComm/Tables/DEG_deseq2.csv", header = FALSE)
